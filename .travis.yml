sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sudhir/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sudhir/react-test npm test

after_success:
  - docker build -t sudhir/multi-client ./client
  - docker build -t sudhir/multi-nginx ./nginx
  - docker build -t sudhir/multi-server ./server
  - docker build -t sudhir/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub 
  - docker push sudhir/multi-client
  - docker push sudhir/multi-nginx
  - docker push sudhir/multi-server
  - docker push sudhir/multi-worker
